{
  "providers": {
    "data": {
      "type": "json",
      "base_path": "${DATA_PATH:./default_data}"
    },
    "audio": {
      "type": "forvo",
      "api_key": "${FORVO_API_KEY:test_key}"
    },
    "sync": {
      "type": "anki"
    }
  },
  "system": {
    "debug": "${DEBUG_MODE:false}"
  }
}
